# Frontend (Vite) — copy to `.env.local` for local dev (gitignored via *.local).
# This file is a safe template; put real values only in `.env.local` or Render Static Site env.
# TrustedHome Express API (see /server)
VITE_API_URL=http://localhost:4000/api

# Google Sign-In (Web client ID from Google Cloud Console → APIs & Services → Credentials)
# VITE_GOOGLE_CLIENT_ID=xxxxx.apps.googleusercontent.com
